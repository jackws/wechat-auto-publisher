# 微信公众号自动发布系统

AI 驱动的微信公众号自动发布系统，独立运行无需 n8n。简洁的单页 Web 界面，支持 AI 写文章、多源配图、定时发布，专为非技术用户设计。

## 功能特性

- **一键写文**：输入主题，AI 自动生成公众号文章，SSE 流式实时预览
- **智能配图**：Pexels + Bing 图片搜索 + 智谱 CogView + 通义万相 AI 生图 + 自定义端点
- **微信草稿箱发布**：自动获取 access_token、上传封面图、创建草稿
- **定时发布**：node-cron 定时任务，支持每天/工作日/周末三种频率
- **分步教程**：每个配置项都有详细的分步获取指南，小白也能轻松上手
- **管理员面板**：密码保护的管理员设置弹窗，普通用户无感
- **单页设计**：所有操作在一个页面完成，无需页面跳转

## 快速开始

### 方式一：Docker

```bash
git clone https://github.com/jackws/wechat-auto-publisher.git
cd wechat-auto-publisher
cp .env.example .env
# 编辑 .env 填入你的配置
docker compose up -d
```

### 方式二：本地运行

```bash
git clone https://github.com/jackws/wechat-auto-publisher.git
cd wechat-auto-publisher
npm install
cp .env.example .env
# 编辑 .env 填入你的配置
npm start
```

访问 http://localhost:3000 进入使用界面。

## 使用流程

1. **绑定公众号**：填写微信公众号的 AppID 和 AppSecret（点击「如何获取？」查看分步教程）
2. **输入主题**：在输入框中输入文章主题，点击「生成文章」
3. **流式预览**：实时查看 AI 生成的文章内容
4. **选择配图**：从自动匹配的图片中选择封面图
5. **发布到草稿箱**：一键发布到微信公众号草稿箱
6. **管理员设置**：点击右上角齿轮图标，输入密码进入管理面板，配置 AI 模型、图片来源、定时任务等

## 配置说明

所有配置均可通过管理面板的 Web UI 设置，也可通过环境变量预设。

管理员面板入口：页面右上角齿轮图标，默认密码 `hrj5201314`

| 变量 | 必填 | 说明 |
|------|------|------|
| `AI_PROVIDER_URL` | 否 | AI API 地址，默认小米 MIMO |
| `AI_API_KEY` | 否 | AI API 密钥 |
| `AI_MODEL` | 否 | AI 模型名称，默认 `mimo-v2.5-pro` |
| `WECHAT_APP_ID` | 否 | 微信公众号 AppID |
| `WECHAT_APP_SECRET` | 否 | 微信公众号 AppSecret |
| `IMAGE_SOURCES` | 否 | 启用的图片来源，逗号分隔：`pexels,bing,cogview,tongyi,custom-ai` |
| `PEXELS_API_KEY` | 否 | Pexels 图片 API 密钥 |
| `BING_KEY` | 否 | Bing 图片搜索 API Key（Azure） |
| `COGVIEW_KEY` | 否 | 智谱 CogView API Key |
| `TONGYI_KEY` | 否 | 阿里云通义万相 API Key |
| `IMAGE_AI_COUNT` | 否 | AI 生图数量，默认 2 |
| `PORT` | 否 | 应用端口，默认 `3000` |

## 图片源说明

| 来源 | 类型 | 中国大陆 | 说明 |
|------|------|----------|------|
| Pexels | 图库搜索 | 可访问 | 免费高质量图库 |
| Bing 图片 | 图库搜索 | 可访问 | 需 Azure 订阅 |
| 智谱 CogView | AI 生图 | 可访问 | 根据文章内容生成 |
| 通义万相 | AI 生图 | 可访问 | 阿里云服务 |
| 自定义端点 | AI 生图 | 自行部署 | POST `{"prompt":"..."}` → `{"url":"..."}` |

## 技术架构

```
┌──────────────────────────────────────────────────┐
│               单页 Web 界面                       │
│  ┌────────────────────────────────────────────┐  │
│  │  4 步写作流程：绑定 → 输入主题 → 生成 → 发布 │  │
│  │  发布历史（折叠）                            │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │  管理员弹窗 (密码保护)                      │  │
│  │  AI 配置 | 公众号 | 图片源 | 定时 | 提示词   │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │  帮助面板 (从右侧滑入)                      │  │
│  │  分步骤图文教程                             │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────┐
│           Express.js 后端                        │
│  ┌────────────────────────────────────────┐     │
│  │  SSE 流式生成 → AI API → 解析截断      │     │
│  │  多源配图 → 排序合并                    │     │
│  │  微信 API → access_token → 上传 → 草稿  │     │
│  │  node-cron 定时触发                    │     │
│  └────────────────────────────────────────┘     │
│  data/config.json   data/history.json            │
└──────────────────────────────────────────────────┘
```

## 许可证

MIT
